class LinkedList {

/**
 * It returns an object with two properties, head
 * and tail, both undefined
 */
const LinkedList
  = head =>
    tail =>
      ( {
        head,
        tail,
      } );

    this.head = undefined;
    this.tail = undefined;

  }

}
